version: '3.8'

services:
  bot:
    build: .
    container_name: football-qr-bot
    restart: always
    env_file:
      - .env
    # No ports needed for polling bot
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
